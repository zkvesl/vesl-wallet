[toolchain]
# Pinned to stable. The whole point of the math-substrate vendoring decision
# is to keep this crate stable-clean: hardware-wallet vendors and external
# consumers should not need a nightly toolchain to depend on us.
channel = "stable"
components = ["rustfmt", "clippy"]
