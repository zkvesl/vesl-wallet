/target
**/*.rs.bk

# Editor cruft
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Local developer guidance (Claude Code reads this when working here;
# not part of the shipped repo — matches vesl-core / vesl-nockup /
# hull-llm convention).
CLAUDE.md

# Local working notes — deferred-work TODOs and audit issue docs.
# Not shipped; matches the vesl-core / vesl-nockup / hull-llm convention.
.dev/

# Cargo.lock IS committed for vesl-wallet (workspace-of-libraries-with-CI
# is the canonical bundle, reproducible CI matters). If hardware-wallet
# vendors complain about noise downstream we revisit at v0.2.0.

# Release tooling (maintainer-local; release.yml workflow stays tracked)
/scripts/release.sh
/scripts/release-notes.template.md
